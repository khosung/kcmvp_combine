# kcmvp_combine requirements
# docling 2.80.0 기준 — Fix17 패치가 이 버전의 내부 파일 구조에 맞춰 작성됨
# 버전이 다르면 patches/ 파일을 다시 생성해야 합니다

docling==2.80.0

# PDF 페이지 수 파악 (청크 변환 시 사용) — 선택 사항
# 없어도 동작하지만, 45페이지 이상 PDF에서 청크 분할이 작동하지 않음
PyMuPDF>=1.23.0
